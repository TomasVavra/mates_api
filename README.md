1)
install playwright and BeautifulSoup python packages
Run this in your project folder:

python3 -m venv .venv
source .venv/bin/activate
pip install playwright
playwright install
pip install beautifulsoup4


2) create login.txt in project folder
username=myuser
password=testpass
