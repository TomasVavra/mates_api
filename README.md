1)
install playwright python package
Run this in your project folder:
python3 -m venv .venv
source .venv/bin/activate
pip install playwright
playwright install

2) create login.txt in project folder
username=myuser
password=testpass
